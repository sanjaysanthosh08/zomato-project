# Database Configuration
spring.application.name=zomato
spring.datasource.url=jdbc:mysql://localhost:3306/zomato
spring.datasource.username=root
spring.datasource.password=sanjaysanthosh08

# Hibernate Config
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Server Port
server.port=8080

# JWT Secret (MANDATORY FOR JWT TO WORK)
jwt.secret=my_jwt_secret_key_which_is_at_least_32_characters_long
